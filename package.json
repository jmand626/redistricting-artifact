{
  "name": "redistricting-artifact",
  "version": "2.0.0",
  "private": true,
  "description": "An interactive toy-data sandbox for exploring redistricting, representation, and metric tradeoffs.",
  "homepage": "https://jmand626.github.io/redistricting-artifact/",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "npm run test && npm run build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "gh-pages": "^6.2.0",
    "vite": "^5.4.11"
  }
}
